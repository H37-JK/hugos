# baseURL은 지우거나 아래처럼 더미 주소를 넣으세요 (파이썬에서 -b로 덮어씌울 겁니다)
languageCode = 'ko-kr'
title = 'homecare'

# 아래 옵션들이 중요합니다
canonifyURLs = true     # 주소를 절대 경로로 강제 변환
relativeURLs = false    # 상대 경로 사용 안 함
disableKinds = ["taxonomy", "term"] # 불필요한 페이지 생성 방지 (선택사항)